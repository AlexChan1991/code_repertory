 //度数转文字的函数
    public function WindDirection($d) {
            $WD =Array("北", "东北", "东", "东南", "南", "西南", "西", "西北");
            // echo floor(((intval($d) + 22.5) % 360) / 45);exit();
            return $WD[floor(((intval($d) + 22.5) % 360) / 45)];
        }
